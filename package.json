{
  "name": "licona-saleor-apps",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present --no-bail test",
    "typecheck": "pnpm -r exec tsc --noEmit"
  }
}
